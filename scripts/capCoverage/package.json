{
  "name": "script-creazione-cap-coverage",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "sample": "node index.js data/sample.csv --api-url https://api.example.com"
  },
  "keywords": ["csv", "api", "coverage", "post-requests"],
  "author": "Alessandro Masci",
  "license": "ISC",
  "description": "Node.js script to read CSV data and create coverage entries via API calls",
  "dependencies": {
    "axios": "^1.12.2",
    "csv-parser": "^3.2.0",
    "dotenv": "^17.2.3",
    "@aws-sdk/client-cognito-identity-provider": "^3.645.0"
  }
}
